#!r6rs

;;
;; This library supports the parallel-comm library.  We serialize
;; objects before sending them across a communication channel.
;;
;; D. Wingate 2/10/09
;;

(library (church utils serializer)

  (export serialize deserialize)
  (import (mzlib serialize))

)